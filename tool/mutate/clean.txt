# Source files verified at a 100% mutation score. A file joins this list in the
# PR that brings it to zero survivors. See docs/mutation-testing.md.
#
# Touch a file that is NOT here and the gate mutates it in full, because it has
# no proof the untouched lines are pinned. Touch one that IS here and the gate
# mutates only your changed lines. Adding an entry is therefore a claim CI
# re-checks every time that file is next touched.
src/agent_apropos/matcher.cr
src/agent_apropos/version.cr
src/agent_apropos/rendering.cr
src/agent_apropos/agents/agent.cr
