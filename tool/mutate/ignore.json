{
  "note": "Reviewed equivalent mutants: a mutant that CANNOT change observable behaviour. Anything that could behave differently gets a fix or a pinning spec instead. `occurrence` is the 1-based index of `original` among identically-trimmed lines in the file; text is trimmed, with an inserted line folded on as a literal \\n. See docs/mutation-testing.md.",
  "equivalent": [
    {
      "path": "src/agent_apropos/rendering.cr",
      "occurrence": 1,
      "original": "def context(docs : Array({String, String})) : String",
      "mutated": "def context(docs : Array( { String,String})) : String",
      "reason": "whitespace inside a type annotation; the argument-swap operator moved spaces, not arguments. The tuple's element types stay in the same positions, so the declared type is identical."
    },
    {
      "path": "src/agent_apropos/rendering.cr",
      "occurrence": 1,
      "original": "private def summarized(docs : Array({String, String})) : String",
      "mutated": "private def summarized(docs : Array( { String,String})) : String",
      "reason": "the same whitespace-only type-annotation shuffle as the context entry above; the declared type is unchanged."
    },
    {
      "path": "src/agent_apropos/rendering.cr",
      "occurrence": 1,
      "original": "body.split(\"\\n\\n\", 2).first.strip",
      "mutated": "body.split(\"\\n\\n\", (2+1)).first.strip",
      "reason": "only .first is read, so every limit of 2 or more yields the same element; 2 is simply the cheapest of them."
    },
    {
      "path": "src/agent_apropos/frontmatter.cr",
      "occurrence": 1,
      "original": "KNOWN_KEYS = [\"paths\", \"contents\", \"skill\", \"description\", \"lint\", \"on\"]",
      "mutated": "KNOWN_KEYS =  [ \"contents\",\"paths\", \"skill\", \"description\", \"lint\", \"on\"]",
      "reason": "KNOWN_KEYS is only ever consulted with Array#includes?, which does not depend on element order; swapping two entries (plus the argument-swap operator's incidental whitespace) cannot change which keys are reported unknown."
    },
    {
      "path": "src/agent_apropos/frontmatter.cr",
      "occurrence": 1,
      "original": "KNOWN_KEYS = [\"paths\", \"contents\", \"skill\", \"description\", \"lint\", \"on\"]",
      "mutated": "KNOWN_KEYS = [\"paths\" , \"skill\", \"contents\", \"description\", \"lint\", \"on\"]",
      "reason": "same order-independence as the entry above, swapping a different pair of entries."
    },
    {
      "path": "src/agent_apropos/frontmatter.cr",
      "occurrence": 1,
      "original": "KNOWN_KEYS = [\"paths\", \"contents\", \"skill\", \"description\", \"lint\", \"on\"]",
      "mutated": "KNOWN_KEYS = [\"paths\", \"contents\" , \"description\", \"skill\", \"lint\", \"on\"]",
      "reason": "same order-independence as the entry above, swapping a different pair of entries."
    },
    {
      "path": "src/agent_apropos/frontmatter.cr",
      "occurrence": 1,
      "original": "KNOWN_KEYS = [\"paths\", \"contents\", \"skill\", \"description\", \"lint\", \"on\"]",
      "mutated": "KNOWN_KEYS = [\"paths\", \"contents\", \"skill\" , \"lint\", \"description\", \"on\"]",
      "reason": "same order-independence as the entry above, swapping a different pair of entries."
    },
    {
      "path": "src/agent_apropos/frontmatter.cr",
      "occurrence": 1,
      "original": "KNOWN_KEYS = [\"paths\", \"contents\", \"skill\", \"description\", \"lint\", \"on\"]",
      "mutated": "KNOWN_KEYS = [\"paths\", \"contents\", \"skill\", \"description\" , \"on\", \"lint\"]",
      "reason": "same order-independence as the entry above, swapping the last two entries."
    },
    {
      "path": "src/agent_apropos/frontmatter.cr",
      "occurrence": 1,
      "original": "unknown = hash.keys.compact_map(&.as_s?).reject { |key| KNOWN_KEYS.includes?(key) }.sort!",
      "mutated": "unknown = hash.keys.compact_map(&.as_s?).reject { |key| KNOWN_KEYS.includes?(key) }.sort",
      "reason": "the receiver is a freshly-built Array with no other reference, and the result is immediately reassigned to `unknown`; `sort` (allocate-and-return) and `sort!` (sort-in-place-and-return) leave `unknown` holding the identical sorted contents either way."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "capture(repo_root, [\"diff\", \"--no-color\", range])",
      "mutated": "capture(repo_root, [\"diff\" , range, \"--no-color\"])",
      "reason": "confirmed against a real repo: git accepts `--no-color` in any position relative to the revision range on `git diff`, so this reordering invokes the identical command."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "capture?(repo_root, [\"symbolic-ref\", \"--short\", name]).try(&.strip).presence",
      "mutated": "capture?(repo_root, [\"symbolic-ref\" , name, \"--short\"]).try(&.strip).presence",
      "reason": "confirmed against a real repo: git accepts `--short` before or after the ref name on `git symbolic-ref`, so this reordering invokes the identical command."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "!capture?(repo_root, [\"rev-parse\", \"--verify\", \"--quiet\", ref]).nil?",
      "mutated": "!capture?(repo_root, [\"rev-parse\", \"--verify\" , ref, \"--quiet\"]).nil?",
      "reason": "confirmed against a real repo: git accepts `--quiet` before or after the ref on `git rev-parse --verify`, so this reordering invokes the identical command."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "!capture?(repo_root, [\"rev-parse\", \"--verify\", \"--quiet\", ref]).nil?",
      "mutated": "!capture?(repo_root, [\"rev-parse\" , \"--quiet\", \"--verify\", ref]).nil?",
      "reason": "confirmed against a real repo: git accepts `--verify` and `--quiet` in either order on `git rev-parse`, so this reordering invokes the identical command."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "!capture?(repo_root, [\"rev-parse\", \"--verify\", \"--quiet\", ref]).nil?",
      "mutated": "!capture?(repo_root, [\"rev-parse\", \"--quiet\", ref]).nil?",
      "reason": "`ref_exists?` only inspects success/failure via `.nil?`, never the exit code's exact value or stdout content; confirmed against a real repo across an existing ref, a nonexistent one, an empty string, and a flag-like ref name that `--verify`/`--quiet` alone, both, or neither agree on the resulting zero/nonzero exit status every time, so dropping `--verify` cannot change the boolean this method reports."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "!capture?(repo_root, [\"rev-parse\", \"--verify\", \"--quiet\", ref]).nil?",
      "mutated": "!capture?(repo_root, [\"rev-parse\", \"--verify\", ref]).nil?",
      "reason": "same success/failure-only observation as the entry above; dropping `--quiet` only changes discarded stderr noise, never the exit status `ref_exists?` reports on."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "output = capture?(repo_root, [\"status\", \"--porcelain\", \"-z\", \"--untracked-files=no\"])",
      "mutated": "output = capture?(repo_root, [\"status\", \"--porcelain\", \"-z\"])",
      "reason": "confirmed against a real repo: an untracked file only ever appears with a `??` status, which `tracked_removal_status?` never matches, and a deleted-and-never-tracked file produces no status line at all whether or not this flag is passed — so it changes only the cost of the untracked-tree walk `--untracked-files=no` is there to skip, never the array `removed_paths` returns."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "output = capture?(repo_root, [\"status\", \"--porcelain\", \"-z\", \"--untracked-files=no\"])",
      "mutated": "output = capture?(repo_root, [\"status\" , \"-z\", \"--porcelain\", \"--untracked-files=no\"])",
      "reason": "`--porcelain` and `-z` are independent flags to `git status`; git accepts them in either order, so this reordering invokes the identical command."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 1,
      "original": "!capture?(repo_root, [\"rev-parse\", \"--verify\", \"--quiet\", ref]).nil?",
      "mutated": "!capture?(repo_root, [\"rev-parse\", ref]).nil?",
      "reason": "same success/failure-only observation as the two narrower `--verify`/`--quiet` entries above; dropping both at once is still only a change to discarded stderr noise and exit-code magnitude, never its zero/nonzero-ness for any ref this method has been exercised with."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 9,
      "original": "end",
      "mutated": "next\\nend",
      "reason": "closes the `if record[0]? == 'R' || record[0]? == 'C'` branch in `parse_removed_records`, whose last statement is already `candidates << source if source`; `next` there is indistinguishable from normal fall-through to the until loop's own condition check."
    },
    {
      "path": "src/agent_apropos/git.cr",
      "occurrence": 10,
      "original": "end",
      "mutated": "next\\nend",
      "reason": "closes `parse_removed_records`'s `until records.empty?` loop itself; `next` as the loop body's last statement jumps to the condition check, which is exactly what reaching this `end` already does."
    }
  ]
}
